# name: Auto-approve Codex reviews (PR changes)

# on:
#   pull_request:
#     types: [opened, reopened, edited, synchronize]

# permissions:
#   pull-requests: write

# jobs:
#   auto-approve-on-pr-change:
#     runs-on: ubuntu-latest
#     steps:
#       - name: Scan PR comments for bot approve triggers and auto-approve
#         uses: actions/github-script@v6
#         env:
#           CODEX_BOT_LOGINS: codex[bot]
#           APPROVE_EMOJIS: '👍'
#         with:
#           script: |
#             const botLoginsEnv = (process.env.CODEX_BOT_LOGINS || 'codex[bot]').split(',').map(s => s.trim()).filter(Boolean).map(s=>s.toLowerCase());
#             const emojisEnv = (process.env.APPROVE_EMOJIS || '👍').split(',').map(s => s.trim()).filter(Boolean);
#             const owner = context.repo.owner;
#             const repo = context.repo.repo;
#             const pr = context.payload.pull_request;
#             if (!pr) {
#               console.log('No pull_request payload — exiting.');
#               return;
#             }
#             const pull_number = pr.number;

#             const commentMatches = (c) => {
#               if (!c || !c.user || !c.user.login || !c.body) return false;
#               const login = c.user.login.toLowerCase();
#               if (!botLoginsEnv.includes(login)) return false;
#               return emojisEnv.some(e => c.body.includes(e));
#             }

#             const commentsRes = await github.rest.issues.listComments({ owner, repo, issue_number: pull_number });
#             const comments = commentsRes.data || [];
#             const matched = comments.find(c => commentMatches(c));
#             if (!matched) {
#               console.log('No matching bot comment with trigger found on PR. Exiting.');
#               return;
#             }
#             console.log(`Found matching bot comment by ${matched.user.login}.`);

#             // Avoid duplicate approvals
#             const reviewsRes = await github.rest.pulls.listReviews({ owner, repo, pull_number });
#             const alreadyApproved = reviewsRes.data.some(r => r.user && r.user.login && botLoginsEnv.includes(r.user.login.toLowerCase()) && r.state === 'APPROVED');
#             if (alreadyApproved) {
#               console.log('PR already approved by one of configured bots. Exiting.');
#               return;
#             }

#             await github.rest.pulls.createReview({ owner, repo, pull_number, event: 'APPROVE', body: `Auto-approved because ${matched.user.login} commented with one of ${emojisEnv.join(', ')}.` });
#             console.log('Created APPROVE review on PR.');
